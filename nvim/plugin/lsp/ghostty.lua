-- Ghostty terminal's bundled Vim plugin — local dir plugin, not managed by vim.pack
-- Add its path directly to runtimepath so Neovim can source it
vim.opt.rtp:append("/Applications/Ghostty.app/Contents/Resources/vim/vimfiles/")
